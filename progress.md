Original prompt: Quando nÃƒÂ£o tem ID na ficha, o sistema nÃƒÂ£o devolve o pokÃƒÂ©mon corretamente, o sprite na aba de ficha

- InvestigaÃƒÂ§ÃƒÂ£o inicial: projeto localizado em `battle-site/`; bug reportado na aba de fichas quando a ficha nÃƒÂ£o possui ID.
- PrÃƒÂ³ximo passo: localizar a resoluÃƒÂ§ÃƒÂ£o de sprite/dados do PokÃƒÂ©mon para a ficha selecionada e aplicar fallback por nome/espÃƒÂ©cie.
- CorreÃƒÂ§ÃƒÂ£o aplicada em `main.js`: a aba de fichas e o inspector agora resolvem a ficha por `pokemon.id`, `linked_pid`, PID da party e `pokemon.name`, nessa ordem, em vez de depender sÃƒÂ³ do ID da ficha.
- Ajuste de sprite: `_artUrlFromPidForSheets` agora aceita nome/forma direta, e os renders de card/detalhe usam fallback por nome quando o ID vier vazio ou `0`.
- Ajuste de estado/seleÃƒÂ§ÃƒÂ£o: HP, shiny e seleÃƒÂ§ÃƒÂ£o da ficha passaram a usar a chave resolvida da ficha, evitando cair no placeholder de pokÃƒÂ©bola quando o documento nÃƒÂ£o traz ID vÃƒÂ¡lido.
- ValidaÃƒÂ§ÃƒÂ£o: `node --check main.js` sem erro; pÃƒÂ¡gina abriu em `http://127.0.0.1:4173/index.html` sem erros no console.
- Novo trabalho: iniciativa e scoreboard agora sincronizam a rodada atual apenas com peÃƒÂ§as realmente em campo, removendo da vez quem saiu do tabuleiro sem reordenar a rodada em andamento.
- Virada de rodada ajustada em `main.js` e `preprep-patch.js`: a prÃƒÂ³xima rodada reconstrÃƒÂ³i `turn_state.order` a partir do tabuleiro atual + `battle.initiative`, entÃƒÂ£o mudanÃƒÂ§as feitas na aba de iniciativa passam a valer no inÃƒÂ­cio da rodada seguinte.
- ValidaÃƒÂ§ÃƒÂ£o atual: parse de `main.js`, `scoreboard-patch.js` e `preprep-patch.js` confirmado com `node --experimental-vm-modules`; nÃƒÂ£o consegui validar no navegador porque o `http.server` nÃƒÂ£o subiu dentro do sandbox.
- Ajuste extra no scoreboard: o patch agora usa `public_state/state.seen` para manter o sprite visÃƒÂ­vel depois que um pokÃƒÂ©mon jÃƒÂ¡ foi revelado, mesmo fora do campo.
- Novo trabalho: a aba `Fichas` agora lÃª `moves[].notes` direto da ficha persistida e mostra o texto abaixo da descriÃ§Ã£o do golpe.
- Ajuste de UX em `main.js`: o inspector espelhado da aba `Fichas` passou a iniciar com os golpes recolhidos, alinhando o comportamento com o detalhe principal da aba.
- ValidaÃ§Ã£o atual: `node --check main.js` sem erro. NÃ£o consegui fechar a validaÃ§Ã£o visual no Playwright porque o servidor HTTP temporÃ¡rio derrubado pelo shell respondeu `ERR_EMPTY_RESPONSE` para `main.js` e os patches antes do app concluir a carga.
- Novo trabalho: held item agora usa `rooms/{rid}/players/{uid}.party_snapshot[].held_item` como fonte da UI de batalha.
- Ajuste em `main.js`: adicionei lookup por `party_snapshot`, badge/tooltip reutilizavel para item e fetch lazy do efeito via PokeAPI usando `held_item.api_name`.
- Ajuste em `main.js`: o slot da party, o inspector da arena e o detalhe da aba `Fichas` agora mostram item equipado; inspector/ficha tambem exibem o texto do efeito.
- Ajuste em `scoreboard-patch.js`: o lineup superior agora mostra o icone do item para Pokemon do proprio jogador e para Pokemon revelados.
- Validacao atual: Playwright abriu `http://127.0.0.1:4173/index.html`, renderizei uma `party_snapshot` falsa com `held_item`, confirmei o badge no slot, no scoreboard e no inspector.
- Validacao atual: no hover do badge do inspector, o tooltip ficou com `display:block` e exibiu nome + efeito; com `held_item` sem campo `effect`, o texto saiu de `Carregando efeito...` para o retorno de `https://pokeapi.co/api/v2/item/leftovers`.
- Risco residual: `node --check` continua bloqueado pelo sandbox com `EPERM` no path espelhado do workspace, entao a verificacao de sintaxe ficou coberta pela carga real no navegador.
- Correcao extra: o erro `ReferenceError: heldItem is not defined` na aba `Fichas` foi corrigido declarando o valor antes do template da ficha.
- Correcao extra em `main.js`: a resolucao do item agora cai para `getPartyForTrainer()` preservando metadados e para `selfUserData/userProfiles.raw.data.hub_pokemon_meta[pid].held_item` quando o `party_snapshot` da sala vier sem item.
- Validacao atual: revalidei no Playwright um cenario com `party_snapshot` sem `held_item` e apenas `hub_pokemon_meta`, e o item voltou a aparecer no slot da party, no inspector e no scoreboard.
- Novo trabalho: arena voltou para arquitetura `canvas-first`, com fallback DOM controlado por um modo explicito em `main.js`.
- Ajuste em `main.js`: adicionado controlador de render (`arenaRenderMode`, `activateCanvasMode`, `activateDomMode`, `requestArenaRefresh`) para impedir que callbacks assincronos promovam o DOM enquanto o canvas esta saudavel.
- Ajuste em `main.js`: `renderArenaDom()` virou renderer puro do fallback; ele nao esconde mais o canvas e o loop `draw()` agora captura erro de frame e cai para DOM uma unica vez com log/aviso.
- Ajuste em `main.js`: expostas APIs `window.getArenaRenderMode`, `window.getArenaBoardLayout` e `window.requestArenaRefresh` para os patches externos.
- Ajuste em `field-zones-patch.js` e `drawing-patch.js`: overlays passaram a consumir o layout compartilhado da arena; o desenho livre agora alinha o canvas ao board visivel e usa coordenadas de grade consistentes em canvas e DOM.
- Ajuste em `index.html`: cache-bust de todos os modulos locais atualizado para `v=20260409d` para evitar mistura de `main.js` novo com patches antigos.
- Validacao atual: `node --check main.js` sem erro.
- Validacao atual: `node --check` dos patches com import remoto segue falhando com `EPERM` do sandbox em `C:\Users\joaop`, entao a verificacao deles foi feita pela versao servida via `http://127.0.0.1:4173/`.
- Validacao atual: `Invoke-WebRequest` confirmou `index.html` servindo `main.js`, `field-zones-patch.js` e `drawing-patch.js` com `v=20260409d`, e os arquivos servidos contem as novas APIs/layout compartilhado.
- 2026-04-09: hover card direito da arena foi restaurado; a regressao vinha de um `display:none !important` no CSS enquanto o renderer JS continuava ativo. O hover em canvas voltou a aparecer com tipos, matchup ofensivo/defensivo e deslocamento, mantendo a sidebar fixa esquerda para ficha resumida por clique.
- 2026-04-09: animação de entrada/saída das peças foi trocada para um efeito de feixe de Pokébola em `index.html`, e o fallback DOM agora também participa do tracking de presença (`updatePieceFieldFx` em `renderArenaDom`) com ghosts de saída renderizados em uma camada de FX separada do overlay de sprites.
- 2026-04-09: modal de Condições foi refeita em `main.js` com header da peça, cards por grau, descrição estável em painel lateral e resumo único das condições aplicadas; a persistência continua usando `setPieceConditions` sem mudar o contrato de dados.
- 2026-04-09: aba `Fichas` voltou ao layout em duas colunas; `renderSheetsTab()` passou a reutilizar `renderSheetsInspectorCard()` na coluna da direita, recuperando a ficha rica (`ficha-v2`) em vez do detalhe simplificado abaixo dos cards.
- 2026-04-09: validação concluída com `node --check main.js` e com Playwright via script inline usando o pacote global em `C:\\Users\\joaop\\AppData\\Roaming\\npm\\node_modules\\playwright`. Capturas geradas em `output/arena-hover-own-20260409.png`, `output/arena-conditions-20260409.png` e `output/sheets-tab-20260409.png` confirmam hover lateral, modal de condições redesenhada e ficha completa rica na direita do tab.
- 2026-04-09: limitação residual: a nova animação de feixe de Pokébola foi validada só por código/CSS e não por captura frame-a-frame do movimento de entrada/saída; se o usuário notar timing estranho, revisar apenas os keyframes `pieceFieldIn` e `pieceFieldOut`.
- 2026-04-09: corrigido o espaço vazio acima do topo da HUD removendo o `margin-top: 72px` redundante do `#scoreboard` em `scoreboard-patch.js`; o header já era compensado por `hero-spacer`.
- 2026-04-09: a arena passou a usar a altura útil real da viewport (`--hud-viewport-height`) em `index.html`, em vez de um `76vh` fixo; isso evita a sensação de “zoom” inicial no mapa quando scoreboard/header mudam de tamanho.
- 2026-04-09: adicionado `scheduleViewportStabilization()` em `main.js` para reexecutar `updateHudViewportHeight()`, `resizeCanvasToContainer()`, `fitToView()` e `renderSheetsTab()` após troca de aba, mudança do scoreboard, carregamento da página e resolução de fontes. Isso corrige o primeiro render ficar dimensionado grande demais.
- 2026-04-09: a ficha completa do tab `Fichas` foi compactada visualmente na coluna da direita com limites de altura/scroll e overrides de tipografia, arte e stat grid, mantendo o visual rico mas sem exigir zoom out do navegador.
- 2026-04-09: validação visual extra concluída com Playwright em `http://127.0.0.1:4173/index.html?rid=949` (`Ezenek` / senha via prompt). Capturas em `output/arena-layout-fix-20260409.png` e `output/sheets-layout-fix-20260409.png` mostram o topo sem vão extra, mapa mais bem enquadrado e ficha do tab mais compacta.
- 2026-04-16: `canCurrentPlayerStartCombat()` em `main.js` agora aceita `ignoreTurn`, permitindo distinguir combate normal do caso especial fora do turno sem afrouxar o fluxo normal de turnos.
- 2026-04-16: `main.js` ganhou `isPieceFreeMovementEnabled()` e `togglePieceFreeMovement()`. O inspector e `handlePieceMenuAction('movement')` passaram a usar a mesma regra, entÃ£o `Deslocamento livre` liga/desliga fora do turno e preserva a peÃ§a selecionada para reposicionamento livre.
- 2026-04-16: `arena-combat.js` passou a exibir `Ataque fora do turno` no menu contextual do inimigo quando o jogador tem permissÃ£o para agir, mas a vez atual pertence ao oponente; o botÃ£o abre o mesmo overlay de ataque sem exigir turno local.
- 2026-04-16: validaÃ§Ã£o concluÃ­da com `node --check main.js`, `node --check arena-combat.js` e Playwright headless via import global de `C:\\Users\\joaop\\AppData\\Roaming\\npm\\node_modules\\playwright`. O estado simulado confirmou `Deslocamento livre OFF -> ON`, `handlePieceMenuAction('movement', 'mine1')` ativando `freeByPieceId.mine1`, `canCurrentPlayerStartCombat()` falso fora da vez e `canCurrentPlayerStartCombat({ ignoreTurn: true })` verdadeiro, alÃ©m do menu de combate listar `Ataque fora do turno`.
- 2026-04-30: o launcher do Streamlit agora gera link com `connect=1` e token curto `launch`; o battle-site preenche sala/treinador, tenta login direto via token Firebase e entra na arena sem senha quando aberto pelo Streamlit. Validei `node --check main.js`, `python -m py_compile app.py`, link com mapa publicado e entrada local em `http://127.0.0.1:8799/?rid=823&connect=1&trainer=codex&launch=...`.
- 2026-05-01: investigado bug de movimento travado. Causa: `sendMoveSelected()` ainda gravava `MOVE_PIECE` em `rooms/{rid}/actions`, mas o cliente atual nao assina/processa essa fila e nao ha worker no repo consumindo `MOVE_PIECE`; a action ficava `status: new` e `public_state/state.pieces` nunca era atualizado.
- 2026-05-01: corrigido em `main.js`: movimento agora usa `movePieceOnBoard()` com transaction direta em `public_state/state.pieces`, revalidando dono, turno ativo, alcance, meio deslocamento, deslocamento livre, borda do grid e stacking antes de persistir.
- 2026-05-01: validado `node --check main.js` e carga local via Playwright em `http://127.0.0.1:4173/index.html?codex-check=move-fix`; tela inicial carregou com `window.appState`, canvas da arena e zero erros de console. Nao foi feito teste autenticado em sala real.
- 2026-05-01: investigado bug em que o overlay de ataque mostrava range/furtivo mas nenhum golpe. Causa encontrada em `arena-combat.js`: `_calcMoveContext()` referenciava `atkOwner`, variavel inexistente, abortando a renderizacao da lista no primeiro golpe.
- 2026-05-01: corrigido `arena-combat.js` para usar o dono recebido (`by`) ao resolver tipos do atacante; cache-bust atualizado em `arena-combat-patch.js` e `index.html` para evitar carregar o modulo antigo.
- 2026-05-01: validado `node --check arena-combat.js` e `node --check arena-combat-patch.js`; Playwright local simulou overlay de ataque com 3 golpes e confirmou lista renderizada sem `ReferenceError`.
- 2026-05-01: validado login real no site local em `http://127.0.0.1:4173` com sala 949 e treinador existente. Resultado: `authStatus=OK`, `connected=true`, status `conectado`, `Sincronizado`, 4 slots de party carregados e arena em modo `canvas`.
- 2026-05-01: nao havia pecas no tabuleiro da sala 949 durante a validacao (`boardPieces=0`), entao o clique real em alvo inimigo nao foi reproduzido nessa sala. Console sem `ReferenceError/atkOwner`; restou apenas 404/ORB de item sprite/API para `item`/`object-object`, nao bloqueante para login.
- 2026-05-01: corrigido HP local da sala para pecas legadas sem `entry_id`/`party_slot`. `updatePartyStateHp()` agora salva em `rooms/{rid}/public_state/party_states` pelo `pid` quando nao ha entry global, e `main.js` foi cache-bustado para `v=20260501hp3`.
- 2026-05-01: validacao da sala 274 mostrou login do perfil solicitado retornando `WRONG_PASS`; como espectador/usuario de teste, a peca ativa de Ezenek tem `pid=45` sem vinculo de party. Simulando `party_states.Ezenek["45"].hp=5`, `getPartyHp("Ezenek", peca)` passou de 6 para 5 na versao hp3.
- 2026-05-01: corrigido o botao de boost temporario. A grade do painel deixou de usar colunas estreitas fixas que faziam os controles `+/-` transbordarem sobre a celula vizinha; os botoes agora tem hitbox fixa e sem transform no hover. `updateStatBoost()` tambem passou a usar transaction em `party_states` e atualizar o cache local antes do rerender, evitando cliques rapidos perdidos por snapshot atrasado. `main.js` foi cache-bustado para `v=20260501boost1`.
